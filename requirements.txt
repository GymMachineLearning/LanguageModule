# Dependencies for the squat video LLM evaluation pipeline.
#
# google-genai is pinned exactly: the video sampling contract this pipeline
# depends on (Part.media_processing = STATIC together with
# Part.video_metadata.fps) does not exist before 2.22.0, and a silently older
# SDK would drop both parameters without raising — producing runs that look
# configured but sample at the API default of 1 fps.
google-genai==2.22.0

# Everything else takes a floor rather than a pin.
opencv-python>=4.13
pandas>=2.3
numpy>=2.0
pydantic>=2.13
PyYAML>=6.0

# Notebook reporting layer (video_llm_evaluation/notebook_support).
matplotlib>=3.10
pillow>=12.0
ipywidgets>=8.1
ipython>=9.0
